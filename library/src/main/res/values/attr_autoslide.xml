<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="AutoScrollViewPager">
        <!-- 是否开启自动滚动 -->
        <attr name="autoScrollEnable" format="boolean"/>
        <!-- 滚动时间间隔 单位：s -->
        <attr name="timeInterval" format="integer"/>
        <!-- 设置单页指示器是否显示(默认不显示) -->
        <attr name="indictorVisibleInSingle" format="boolean"/>
        <!-- 指示器 Bottom Margin -->
        <attr name="indictorBottomMargin" format="dimension"/>
        <!-- 指示器间隔 -->
        <attr name="indictorSpace" format="dimension"/>
        <!-- 是否显示Indictor -->
        <attr name="indictorVisible" format="boolean"/>
        <!-- 滑动因子 -->
        <attr name="scrollFactor" format="float"/>

        <!-- QuickAutoScrollViewPager -->
        <!-- 指示器半径 -->
        <attr name="asvp_indictor_radius" format="dimension"/>
        <!-- 选中指示器颜色 -->
        <attr name="asvp_selected_indictor_color" format="color"/>
        <!-- 反选指示器颜色 -->
        <attr name="asvp_unselected_indictor_color" format="color"/>
        <!-- 开启循环滚动 -->
        <attr name="asvp_cycle_scrolling_enable" format="boolean"/>
    </declare-styleable>
</resources>